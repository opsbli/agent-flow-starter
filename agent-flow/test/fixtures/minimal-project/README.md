# Minimal Project Fixture

A minimal project skeleton used by agent-flow end-to-end tests.

This simulates a typical small project after `install-agent-flow` has run,
with a `package.json` (Node backend + frontend) and some typical source paths.
