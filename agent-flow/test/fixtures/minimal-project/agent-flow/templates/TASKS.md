# Tasks

## 执行原则

- 每个任务 5-30 分钟内可完成。
- 每个任务必须声明 `read_files` 和 `write_files`。
- 未在 `write_files` 中声明的文件不得修改。
- 每个任务必须有验证命令或验证说明。

## 任务列表

### T001 - {name}

目标：

AC：

read_files：

write_files：

步骤：

验证：

可并行：

### T002 - {name}

目标：

AC：

read_files：

write_files：

步骤：

验证：

可并行：
